import { Movable } from './movable';
/**
 * __Esta es la clase Coche.__
 */
export declare class Coche implements Movable {
    nombre: string;
    velocidad: number;
    constructor(nombre: string, velocidad: number);
}
