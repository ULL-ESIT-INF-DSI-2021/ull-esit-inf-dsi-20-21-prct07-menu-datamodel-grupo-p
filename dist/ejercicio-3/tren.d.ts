import { Movable } from './movable';
/**
 * __Esta es la clase Tren.__
 */
export declare class Tren implements Movable {
    nombre: string;
    velocidad: number;
    constructor(nombre: string, velocidad: number);
}
